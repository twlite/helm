{
  "name": "@helm/guest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --target=bun --bundle --external playwright --outfile=../../runtime/guest/helm-guest.js",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.2"
  }
}
